# spring-rest-maven-docker
dockerized spring boot maven application


To build the project run:
mvn compile
mvn clean install

Then run the application.

you can check the behaviour on localhost:5000
